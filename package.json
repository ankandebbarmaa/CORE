{
  "name": "core",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:admin": "npm run dev --prefix admin",
    "dev:backend": "npm run dev --prefix backend",
    "dev:all": "sh -c \"npm run dev --prefix backend & npm run dev --prefix frontend & npm run dev --prefix admin & wait\"",
    "build": "npm run build --prefix frontend && npm run build --prefix admin",
    "lint": "npm run lint --prefix frontend",
    "check:backend": "npm run check --prefix backend"
  }
}